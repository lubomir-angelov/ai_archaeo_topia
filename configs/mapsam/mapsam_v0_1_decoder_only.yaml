experiment_name: mapsam_v0_1_decoder_only

dataset:
  root: data_lake/curated/datasets/mapsam_v0
  samples: metadata/training_samples.jsonl
  image_size: 1024

model:
  model_type: vit_b
  sam_checkpoint: models/checkpoints/sam/sam_vit_b_01ec64.pth
  freeze_image_encoder: true
  train_prompt_encoder: false
  train_mask_decoder: true

training:
  epochs: 50
  batch_size: 1
  learning_rate: 0.0001
  weight_decay: 0.01
  device: cuda

loss:
  bce_weight: 1.0
  dice_weight: 1.0

outputs:
  root: artifacts/models/mapsam/v0_1_decoder_only
